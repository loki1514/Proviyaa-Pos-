PROVIYAA POS — REQUIREMENT TRACEABILITY

Status values:
COMPLETE = evidence exists in this local repository and the requirement is satisfied for the documented scope.
PARTIAL = boundary/contract/documentation exists but runtime or integration work remains.
BLOCKED = requires missing access, hardware, dependency, implementation or approval.
OPEN = owner decision is required before implementation.

ID | Requirement | Actual source citation | PRD section | Phase | Test/evidence | Status
---|---|---|---|---|---|---
R01 | POS is a separate Restaurant product and does not rebuild the existing OS | Product Architecture §§1, 9, 109; Product Flow §§1, 5; viyaa routes §§1–3 | §§1, 3, 14 | A/B | Separate target folder; no Repo-1- files modified | COMPLETE
R02 | One central Supabase/PostgreSQL commerce system; no copied cloud customer/order/payment database | Product Architecture §§3, 12, 17, 74–75; Product Flow §§3, 28; viyaa routes §§6, 9–11 | §§1, 5 | B/D | Existing-database map/access gate; no new cloud DB; no remote changes | PARTIAL
R03 | Local-first POS uses restricted device SQLite as working store | Product Architecture §71 and §§74–75; Product Flow §§28, 37; PRD §7 | §§1, 6–7 | C | `lib/data/app_database.dart` + `lib/data/drift_local_store.dart` implement `PosLocalStore` on real Drift/SQLite; not yet wired into any order-capture UI | PARTIAL
R04 | Local save has durable pending-upload intent and idempotent replay | Product Architecture §§71, 82, 101; Product Flow §§28–30; PRD §§6, 10–11 | §§6, 10–11 | C/D | `DriftLocalStore` writes a `pending_upload_intents` row per order inside one transaction; replay past the first write is a no-op; verified durable across a close/reopen cycle. `lib/application/sync_worker.dart` now closes the other half: a tested (7 tests) retry/idempotency-aware worker matching PRD §10's outcome contract exactly (accepted/duplicate both synchronize; rejected/conflict need review and don't block the rest of the queue; network failures retry without being misfiled as business rejections). `CommandTransport` is an interface only — no real authorized-command API exists yet (PRD §10 itself: "proposals, not existing endpoints"; D06: Repo-1- owns central contracts) | PARTIAL
R05 | Offline cash is distinct from provider-verified digital payment | Product Architecture §§17, 71, 104; PRD §9 | §9 | C/F | `lib/domain/payment.dart` + `lib/application/payment_service.dart` + `lib/data/drift_payment_store.dart`: a real, own-table `Payments` record (never overloaded onto order status, per PRD §9 "do not overload one status"); idempotent by order id; verified end to end (clicked Complete Payment, confirmed the row on disk via sqlite3). Card/UPI/Split Pay explicitly refuse in the UI rather than being silently treated as verified. Provider integration itself (a real card/UPI processor) is still absent — that's Phase F, not this | PARTIAL
R06 | Menu, modifiers, tables, orders, order lines, snapshots and totals use shared commerce concepts | Product Architecture §§12, 25, 74–75; viyaa routes §9; PRD §4 R03–R05 | §§4, 7 | C | Order-line domain model and totals test; UI/menu/table runtime pending | PARTIAL
R07 | KOT/kitchen output has durable identity and explicit recovery/reprint behavior | Product Architecture §§25, 70, 81, 99, 101; viyaa routes §9; PRD §§4 R06, 10–11 | §§4, 10–11 | C/F | Contract documented; hardware/print adapter not implemented | BLOCKED
R08 | Tenant, organization, location, role and actor scope is enforced by authorized commands/RLS | Product Architecture §§7, 82–84, 102–103; Product Flow §§7, 29; PRD §§5, 10–11 | §§5, 10–11 | B/D | Central RLS/tenant enforcement itself is Repo-1-'s to own (D06, D14) — not built here by design, not just by access limitation. What this repo can and now does own: `lib/application/auth_service.dart` + `lib/data/drift_device_session_store.dart`, a real, tested offline-grant cache (24h per D04) distinguishing "no grant" from "expired grant" per PRD §11. `lib/data/supabase_auth_backend.dart` compiles against the real Supabase Auth API (current SDK, confirmed via docs) using the owner-confirmed project key, but has not been exercised against a live account — no Supabase MCP schema access, and no test credentials were created (this session does not create accounts) | BLOCKED
R09 | OneCity and rider use shared order/fulfilment/delivery records, not restaurant copies | Product Architecture §§45, 51, 77, 91–94, 108; viyaa routes §§10–11; PRD §§3, 9–10 | §§3, 9–10 | E/F | Proposed handoffs/events documented; no remote integration | PARTIAL
R10 | Finance separates payment, refund, cash custody, fees, merchant payable, remittance and settlement | Product Architecture §§17, 104–106; Product Flow §§3, 30; PRD §9 | §9 | F | Requirements captured; finance rules and implementation not approved | BLOCKED
R11 | Approved visual/interaction reference exists and is being implemented 1:1 | `v1/*.png` (36 exported screens, light+dark pairs of 17 real VINII POS screens); PRD §12 | §12 | C | Tables (`tables_screen.dart`) and POS/order-capture (`pos_order_screen.dart`) rebuilt and visually verified against `v1/POS-TABLES-V3.png` and `v1/POS-MAIN-V3.png` — including a real click-through (add items, cart totals compute correctly, Send KOT actually saves the order and creates a kitchen ticket through OrderService/KitchenService, not just a mockup). Exact brand color confirmed by pixel sampling (`#76EC00`); real Inter/Geist fonts bundled locally. Payment (`payment_screen.dart`) rebuilt against `v1/POS-PAYMENT-V3.png` and click-tested end to end: added items, opened Payment, confirmed Card/UPI/Split correctly refuse ("needs provider verification, which isn't connected yet") per PRD §9, completed a Cash payment, watched the order screen reset. Reproduces the reference's own nuance that the order-recap column stays light in both themes while only the payment-method column goes dark. KOT/Kitchen Display (`kitchen_display_screen.dart`) rebuilt against `v1/POS-KOT-KITCHEN-V3.png` and click-tested end to end: created two orders, watched tickets appear in New, advanced one through New→Preparing→Ready→Served and confirmed it dropped off the board and all column counts returned to zero — the real KitchenService transitions, not a mockup. Kitchen ticket states were also corrected to match PAGE-RES-005 in the architecture source (`newTicket`/`preparing`/`ready`/`completed`, replacing the earlier invented `queued`/`served`). Five real bugs were caught by actually running the app rather than trusting the code: a Navigator context bug (wrong BuildContext broke all navigation), a RenderFlex overflow in the order panel header, a ListTile/Material paint-order warning, a minor-units-vs-rupees bug that defaulted Amount Received to ₹52500 instead of ₹525, and a live crash from a stale on-disk ticket whose status string (`"queued"`) no longer matched the corrected enum — all fixed. Takeaway, Dine In board, and the persistent top-nav chrome not yet rebuilt | PARTIAL
R12 | Canonical source files are preserved unchanged with checksums | `docs/SOURCE-MANIFEST.txt`; source copies `Product Flow .converted.txt`, `Product Architecture .converted.txt`, `viyaa routes.converted.txt` | — | A | Manifest and SHA-256 verification | COMPLETE
R13 | Plain-text Restaurant POS PRD preserves substantive requirements | `docs/PRD-DAY-03-RESTAURANT-POS.txt`; linked Day-03 PRD source; task Step 2 | All PRD sections | A | Plain-text PRD present and read completely | COMPLETE
R14 | No production migration; Repo-1- remains central schema/migration owner until transfer | Product Architecture §§74–75, 82; PRD §§5, 7–8, 14; current PREFLIGHT “Remote changes” | §§5, 14 | B | No Supabase project configured; no SQL migration applied | COMPLETE
R15 | Local-demo mode works without cloud credentials and local credentials stay ignored | Product Architecture §§8–10, 35–36; Product Flow §§6, 31; PRD §§7–8; `tool/run_local.sh`, `.env.local.example`, `.gitignore` | §7 | C | Allowlisted `--dart-define` launcher; `.env.local` ignored; no secret scan findings | PARTIAL
R16 | Foundation has automated local persistence/domain/widget checks | Product Architecture §§99, 112; Product Flow §32; PRD §§7, 13 | §§7, 13 | C | `flutter analyze`: no issues; `flutter test`: 7 tests passed (order service ×2, widget shell ×1, Drift/SQLite ×4) | COMPLETE for current scaffold
R17 | SQLite must come from a Flutter app dependency, auto-create on first launch, use versioned migrations; MySQL is prohibited | Product Architecture §71; PRD §7; D10 in docs/DECISIONS-AND-QUESTIONS.txt | §7 | C | `lib/data/app_database.dart` (package:drift 2.35 via package:drift_flutter, `schemaVersion` 1, `MigrationStrategy.onCreate`); auto-creates `proviyaa_pos.sqlite` under the platform app-documents directory on first open; `test/app_database_test.dart` covers first-launch table creation, idempotent replay (order + lines not duplicated), and an interruption/recovery cycle (close mid-shift, reopen, unsynced order and its pending_upload_intent still present); no MySQL installed | COMPLETE
R18 | Source understanding and citation-based traceability are completed before feature work resumes | All six documents listed in `docs/SOURCE-UNDERSTANDING.txt`; `docs/PREFLIGHT.txt` | §§2, 12, 14 | A | `SOURCE-UNDERSTANDING.txt` created; this file updated with actual citations | COMPLETE

IMPLEMENTATION FILES CURRENTLY IN SCOPE
---------------------------------------
- lib/config/app_config.dart
- lib/domain/order.dart
- lib/application/order_service.dart
- lib/data/app_database.dart (Drift schema + generated app_database.g.dart)
- lib/data/drift_local_store.dart (real PosLocalStore, on-disk)
- lib/data/in_memory_local_store.dart (kept for lightweight unit tests)
- lib/data/sqlite_schema.dart (superseded stub — see file header)
- lib/main.dart
- test/order_service_test.dart
- test/widget_test.dart
- test/app_database_test.dart
- tool/run_local.sh

EVIDENCE LIMITS
---------------
- Flutter 3.47.4 / Dart 3.13.3 are installed; `flutter analyze` passes and `flutter test` passes with 7 tests.
- Drift/SQLite (R17) is implemented and tested; it is not yet wired into any order-capture UI because no such UI exists yet (see the Figma boundary question recorded under Q02).
- No Supabase MCP project-scoped read-only inspection has occurred. The staging project reference itself was confirmed by the owner 2026-09-15 (see Q04), but authorized project-scoped read-only access has not been set up.
- Figma context is saved read-only in `docs/FIGMA-DESIGN-CONTEXT.md`; no Figma edits were made. The approved restaurant POS board/node set remains an owner decision.
- No MySQL server was installed or configured.
- No remote GitHub, Supabase, Figma, Vercel or production changes were made in this task.
